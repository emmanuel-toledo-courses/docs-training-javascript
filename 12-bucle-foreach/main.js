const numeros = [100, 0, 30, 235, 123, 534];

// Un foreach, solo funciona con arreglos (array, lista, etc).

numeros.forEach(
    (value, index) => {
        console.log(value, index);
    }
);


// for (let index = 0; index < numeros.length; index++) {
//   console.log(numeros[index]);
// }
